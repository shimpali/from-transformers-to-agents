# Copy this file to .env and fill in your token. .env is gitignored -- never commit it.
#
# Classic personal access token: needs the "gist" scope.
# Fine-grained token: needs the account-level "Gists" permission set to read/write.
# Create one at https://github.com/settings/tokens
GITHUB_TOKEN=
